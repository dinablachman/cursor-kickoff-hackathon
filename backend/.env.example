# Leave GITHUB_TOKEN unset to run against built-in mock GitHub data.
GITHUB_TOKEN=

# Comma-separated origins allowed to call the API.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# SQLite file location (relative paths resolve against backend/).
DATABASE_URL=sqlite:///./bounty_board.db
