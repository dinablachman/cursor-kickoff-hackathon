{
  "name": "Campus Bug Bounty Board",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && . .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI API server on port 8000 (interactive docs at /docs)."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "Vite React test harness on port 5173."
    }
  ],
  "ports": [
    { "name": "backend api", "port": 8000 },
    { "name": "frontend", "port": 5173 }
  ]
}
