services:
  - type: web
    name: campus-bounty-board
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: SEED_ON_START
        value: "1"
      - key: PORT
        value: "8000"
